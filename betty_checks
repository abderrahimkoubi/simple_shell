#include <stdio.h>

/**
 * main - Entry point of the program
 *
 * Return: 0 on success
 */
int main(void)
{
	int num1 = 5;
	int num2 = 3;
	int result = num1 + num2;

	printf("The result is: %d\n", result);

	return (0);
}
